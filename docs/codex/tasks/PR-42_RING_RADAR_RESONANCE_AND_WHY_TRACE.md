# PR-42 — Ring Radar / Resonance / Why Trace

## Objective

Implement and preserve the Ring Radar / Resonance / Why Trace scope as a bounded Odin v7.1 architecture slice. This task extends the existing v7.1 canon without weakening Universal Work, candidate-only output, app-owned apply, Internal Semantic Bus local-only constraints, Shadow Runtime boundaries or the Narrative Aorta compiler discipline.

## Internal Task ID

PR-42

## Primary Files

- docs/RING_RADAR_RESONANCE_V7_1.md, docs/WHY_TRACE_EXPLAINABILITY_V7_1.md
- relevant schemas under `schemas/v7_1/`
- relevant registries under `registries/`
- relevant shadow modules under `odin/shadow_runtime/`
- tests under `tests/`

## Required Behavior

Codex must implement this task as typed contracts, deterministic helper functions, registry entries and tests. Any Q-like system must be neutralized in public naming while preserving the operational semantics: bounded authority, centerline clarity, precompute-first routing, seed/archetype budget discipline, Maria/Michael profile selection and candidate-only final output.

## Forbidden Scope

- no app mutation
- no external send
- no provider call before admissibility
- no persona simulation
- no untyped seed/archetype prose
- no runtime-proof claims
- no weakening of claim boundary
- no bypass of Odin Final Gate

## Definition of Done

- Documentation exists and names the boundary clearly.
- Schema or registry exists when data is exchanged.
- Shadow module exists where runtime behavior is planned.
- Tests cover valid and invalid paths.
- SYSTEM_MAP, FILE_MANIFEST and registries are updated.
- This task is covered by `REAL-PR-14`.

## Codex PR Summary Template

Summary:
- Implemented Ring Radar / Resonance / Why Trace.
- Preserved Odin candidate-only and app-owned authority boundaries.
- Added tests and registry coverage.

Validation:
- `python -m odin.cli validate-all`
- `python -m pytest -q`


## Implementation Notes

This task must be implemented in the same mechanical style as the rest of Odin: contract first, schema second, registry third, shadow module fourth, tests fifth. The implementation must remain boring and deterministic. It must not introduce hidden agent autonomy, implied runtime proof, direct apply, external send or model route shortcuts.

Codex must explicitly preserve the following invariant chain:

```text
Universal Work → Binding → Centerline / Admissibility → Precompute Economy → Route Score → ModelWorkPacket or Hold/Split/Ask → Candidate Artifact → Response Packet
```

The task is incomplete if it only adds prose. It must include enough machine-readable structure for later implementation to follow mechanically. Where the real module is still future work, the shadow module must expose the intended interface and expected return shape. Where the schema is added, at least one valid fixture and one negative condition must be represented through tests or validation rules.

## Review Checklist

- Does this task preserve app-owned state and app-owned apply?
- Does this task preserve candidate-only output?
- Does this task produce typed data rather than free-form prose?
- Does this task explain route decisions or blocked decisions?
- Does this task update Codex task registry, bundle registry and file manifest?
- Does this task keep public terminology neutral while preserving internal semantics?
