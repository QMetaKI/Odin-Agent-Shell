# Fairy / Y* Seed Binding v7.1

Claim boundary: this document defines architecture, contracts, shadow behavior, and Codex build intent only. It does not claim host validation, model inference proof, network operation, deployment, security audit completion, app apply, or full implementation completeness.

## Purpose

Fairy DSL and Y* Native DSL must not float as poetic or symbolic layers. They must bind to operational seeds, archetype roles, Bug6, Q7, and Shadow Runtime contracts.

## Dual binding

Every Fairy node requires:

```text
fairy_node
→ ystar_node
→ active_seed_family
→ archetype_role
→ shadow_contract
→ forbidden claims
```

## Example

```json
{
  "fairy_node": "the_gatekeeper_checks_the_keys",
  "ystar_node": "boundary.validate_binding",
  "seed_family": "boundary",
  "active_seeds": ["claim_boundary", "no_hidden_apply"],
  "archetype_role": "boundary_guard",
  "shadow_contract": "binding_gate",
  "forbidden": ["app_mutation", "external_send"]
}
```

## Y* compile rule

Y* Native DSL must include seed annotations for center, flow, forbidden paths, emitted contracts, and unresolved holes.

## Maria Fairy Spine rule

Maria Fairy Spine may explain system meaning, but must not introduce a new operational claim. It must map to Y* or remain review-only.

## Codex rule

Codex must implement seed binding before expanding Fairy grammar. Wider grammar without typed seed binding is drift.
